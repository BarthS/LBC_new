%form{action: "/page/profil_search", method: "get"}
  %input.form-control{id: "q", name: "q", placeholder: "Search", type: "text"}

.panel.panel-primary.panel-body.YYY
  %table.table.table-striped.table-hover
    %thead
      %tr
        %th
        %th
        %th
          Prénom
        %th
          Nom
        %th
          Promo
        %th
          Mail
        %th
          Profil


    %tbody
      - @users.each do |u|
        %tr
          %td
          %td
            - if u.avatar?
              = image_tag u.avatar.url(:square), width: "50px", height: "50px", class: "img-circle"

          %td.XXX
            = u.prenom
          %td.XXX
            = u.nom
          %td.XXX
            = u.promo
          %td.XXX
            = u.email
          %td
            %a{href: "/profil/#{u.id}"}
              .glyphicon.glyphicon-user.h4




%script $('#l3').attr('class','active')

:css
  .XXX {
    display:table-cell;
    vertical-align:middle;
    float:none;
  }
  .YYY {
    padding:0px;
  }
